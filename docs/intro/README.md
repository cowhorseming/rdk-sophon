# 1. rdk-sophon 项目介绍

> 本文面向**想快速了解 rdk-sophon 是什么**的外部读者（人或 AI）。
> 不讲实现细节，只讲这个系统是什么、由哪些模块组成、跑起来是什么形态。

## 1.1 这个项目是什么

rdk-sophon 是跑在 **RDK 开发板**（地平线 X5 等，aarch64 Linux/Ubuntu）上的**长驻硬件探针守护进程**。一句话定位：

> **板端常驻一个守护进程，持续采集硬件状态、对外暴露 JSON-RPC 接口，让开发机/云端能拉取状态、下发命令。**

它解决三个核心诉求：
1. **硬件状态上报**：板子的温度/CPU/内存/磁盘/网络/BPU 等，周期推送或按需拉取。
2. **远端下发指令**：远程执行板子上的命令（受安全策略约束）。
3. **多形式接入**：同一套能力，命令行 CLI、REST、WebSocket 都能访问。

## 1.2 两个视角

了解一个系统有两个视角，本项目分两篇文档介绍：

| 视角 | 文档 | 回答什么 |
|------|------|---------|
| **功能模块** | [functional.md](functional.md) | 这个系统会做什么、功能上分几块（采集/协议/分发/告警/传输/入口...） |
| **进程模块** | [processes.md](processes.md) | 跑起来是几个进程、各自什么角色、怎么协作、何时起哪些 |

两个视角互补：功能视角讲"能力怎么分"，进程视角讲"部署怎么落"。先看哪个都行。

## 1.3 速记一句话

- **功能上**：8 个功能模块（硬件采集 + RPC 协议 + RPC 分发 + 采集编排 + 会话驱动 + shell 执行 + 传输适配 + 对外入口）。
- **进程上**：4 个二进制，但默认只 `probe-daemon` 一个常驻进程；`probe-http-gateway`/`probe-ws-outbound` 按需起（协议适配器）；`probectl` 是临时工具不常驻。

## 1.4 想深入了解

- 对外怎么用（接口字段/错误码/示例）：[`../contracts/`](../contracts/)。
- 内部怎么实现（各 crate 设计/依赖/约束）：[`../design/`](../design/)。
- 怎么编译部署：[`../../deploy/`](../../deploy/)。
