{
  "name": "rdk-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RDK 多 Agent 编排终端应用",
  "scripts": {
    "start": "node --experimental-strip-types src/api/tui/main.ts",
    "deploy": "bash deploy/install-rdk-agent.sh",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/**/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "typebox": "1.3.7",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "24.12.4",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
